# Table assets

- `icon.png`: placeholder app icon, a generated image with its corners
  cut to transparency. It stands in until a designed icon exists; the
  letterforms and materials are not final. Regenerate the desktop
  icon set from it with `bunx tauri icon assets/icon.png` run from
  `apps/table`, then delete the `android` and `ios` folders it also
  writes.
