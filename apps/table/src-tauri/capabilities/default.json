{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core defaults plus showing itself once the first frame has painted.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-show"]
}
