{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core defaults, showing itself once the first frame has painted, and the native file dialog for opening maps.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-show", "dialog:default"]
}
