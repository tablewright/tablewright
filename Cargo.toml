# Workspace root. Crates join `members` as they are created
# (apps/table/src-tauri first, crates/core in stage 2; see docs/design.md §4, §8).
[workspace]
members = ["apps/table/src-tauri"]
resolver = "2"

[workspace.package]
version = "0.0.0"
edition = "2024"
rust-version = "1.85"
